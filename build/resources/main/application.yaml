spring:
  application:
    name: IngSw2024-jsp-only
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: mysql0214
    url: jdbc:mysql://localhost:3306/Javengers_DB
    username: root
  jpa:
    hibernate:
      ddl-auto:
        update
      dialect:
        org.hibernate.dialect.MySQL8Dialect